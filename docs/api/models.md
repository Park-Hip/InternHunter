# API Models

## Current Chat Models

Current code references these model types:

- `ChatRequest`
- `ChatResponse`
- `Message`
- `ToolCallInfo`

## Core ETL Models

These are used outside the HTTP layer too:

- `RawJob`
- `LLMJobProcess`
- `ProcessedJob`
- `FetchOutcome`
- `FetchStatus`
- `ExtractionResult`

## TODO

- TODO: verify the exact field definitions in `src/core/models/chat.py` before documenting them in full.
